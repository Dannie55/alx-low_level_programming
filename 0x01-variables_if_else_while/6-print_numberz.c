#include <stdio.h>

/** main - Entry point
 *  author: daniel
 *  Return: Always 0 (success)
 */
int main(void)
{
	int dannie;

	for(dannie = 48 ; dannie < 58 ; dannie++)
		putchar(dannie);
	putchar( '\n' );
	return (0);
}
