#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char dan;

	for (dan = 'a'; dan <= 'z'; dan++)
	{
		putchar(dan);
	}
	putchar('\n');
		return (0);
}
