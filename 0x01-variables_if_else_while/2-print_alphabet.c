#include <stdio.h>
/**
 * main - Entry point
 *
 * Author: Daniel
 *
 * Return: 0 (success)
 */
int main(void)
{
	char pam;

	for (pam = 'a'; pam <= 'z'; pam++)
	{
		putchar(pam);
	}
	putchar('\n');
		return (0);
}
